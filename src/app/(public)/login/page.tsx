import Login from "@/modules/Login/pages";

export const metadata = {
  title: "Вхід",
  description: "Сторінка для входу",
};

const LoginPage = () => {
  return <Login />;
};

export default LoginPage;
