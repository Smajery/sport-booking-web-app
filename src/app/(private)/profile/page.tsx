import Profile from "@/modules/Profile/pages";

export const metadata = {
  title: "Профіль",
  description: "Сторінка профілю",
};

const ProfilePage = () => {
  return <Profile />;
};

export default ProfilePage;
