import Schedule from "@/modules/private/owner/Schedule/pages";

export const metadata = {
  title: "Schedule Page",
  description: "Dashboard Schedule page",
};

const SchedulePage = () => {
  return <Schedule />;
};

export default SchedulePage;
